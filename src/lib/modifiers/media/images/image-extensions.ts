/**
 * An enum for specifying image file extensions.
 */

/**
 * An enum representing possible image file extensions.
 */
export enum ImageExtension {
  PNG = 'png',
  JPEG = 'jpeg',
  GIF = 'gif',
  TIFF = 'tiff',
  WEBP = 'webp',
  SVG = 'svg',
}