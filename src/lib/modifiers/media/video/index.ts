/**
 * Importing and re-exporting the video modifier code.
 */

import {
  minifyVideos,
} from './video-minifiers';
import {
  grayscaleVideos,
} from './video-grayscaler';

export {
  minifyVideos,
  grayscaleVideos,
};
